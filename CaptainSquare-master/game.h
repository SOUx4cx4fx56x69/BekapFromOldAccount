#include <ncurses.h>
/*
typedef WINDOW;
typedef chtype;
typedef FILE;
extern FILE *stdscr;
void getmaxyx(WINDOW *win, int y, int x);
int mvaddch(int y, int x, const chtype ch);
//
chtype mvinch(int y,
int x);
*/
